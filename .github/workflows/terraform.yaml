name: "Terraform"
# description: "Run the Terraform `plan` & `apply` commands on project."

on:
  workflow_call:
    inputs:
      terraform_version:
        description: "The version of Terraform command-line to use."
        type: string
        default: "1.1.0"
        required: false
    secrets:
      cli_config_credentials_token:
        required: true

jobs:
  # checkov:
  #   name: "Static code analysis"
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: "Checkout"
  #       uses: actions/checkout@v2

  #     - name: "Run Checkov by bridgecrew.io"
  #       id: checkov
  #       uses: bridgecrewio/checkov-action@master
  #       with:
  #         framework: terraform
  #         directory: terraform
  #         skip_check: CKV_GIT_1

  plan:
    name: "Plan"
    runs-on: ubuntu-latest
    # needs:
    #   - checkov
    steps:
      - name: "Run `checkout` by GitHub Actions™"
        uses: letsrockthefuture/reusable-workflows/templates/checkout@main

      - name: "Set up Terraform Cloud credentials"
        uses: letsrockthefuture/reusable-workflows/templates/terraform@main
        with:
          cli_config_credentials_token: ${{ secrets.cli_config_credentials_token }}

      - name: "Plan"
        uses: letsrockthefuture/reusable-workflows/templates/terraform/plan@main
        id: terraform_plan
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          make init
          make terraform/plan

      - name: "Plan status"
        if: steps.terraform_plan.outcome == 'failure'
        run: |
          exit 1

  # apply:
  #   name: "Apply"
  #   runs-on: ubuntu-latest
  #   needs:
  #     - plan
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   environment:
  #     name: production
  #   steps:
  #     - name: "Run Checkout by Actions"
  #       uses: letsrockthefuture/reusable-workflows/workflows/checkout@main

  #     - name: "Set up Terraform credentials"
  #       uses: letsrockthefuture/reusable-workflows/workflows/terraform@main

  #     - name: "Apply"
  #       uses: letsrockthefuture/reusable-workflows/workflows/terraform/apply@main