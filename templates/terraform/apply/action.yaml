name: "Terraform applying"
description: "Run the Terraform `apply` command on project."

runs:
  using: "composite"
  steps:
    - name: "Apply"
      run: |
        make init
        make terraform/apply
