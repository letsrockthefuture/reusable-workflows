name: "Perform CodeQL analysis"
description: "Run the code scanning analyze tool on the project."

author: "letsrockthefuture"

runs:
  using: "composite"
  steps:
    - name: "Perform CodeQL analysis"
      uses: github/codeql-action/analyze@v1
