name: "Upload SARIF to the code scanning"
description: "Upload the analysis results to the GitHub code scanning."

author: "letsrockthefuture"

# inputs:

runs:
  using: "composite"
  steps:
    - name: "Upload SARIF results to code scanning"
      uses: github/codeql-action/upload-sarif@v1
      with:
        sarif_file: results.sarif
